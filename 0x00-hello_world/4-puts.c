#include <stdio.h>
/**
 * main - prints the line "Programming is like building a multilingual puzzle"
 * Return: 0 if executed succesfuly, non zero otherwise
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
